# HERSALON | Inner Circle — Full page + Depth Shadow Hero

## Run
```bash
npm i
npm run dev
```

## Hero depth shadow
Scroll a bit: the text moves out fast while the shadow lags, spreads (blur), scales up, and fades out.


## Cursor glow
Buttons and cards are wrapped with `GlowCapture` which adds a radial glow that follows the cursor.
